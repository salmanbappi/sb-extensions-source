<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="tachiyomi.animeextension"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="${appName}"
        android:usesCleartextTraffic="true"
        tools:replace="android:allowBackup,android:icon,android:label">

        <!-- Modern extensions-lib v17 metadata. 'aniyomix.name' is used verbatim by
             the loader, so it must be the BARE name (no "Aniyomi: " prefix). -->
        <meta-data
            android:name="aniyomix.name"
            android:value="${extName}" />
        <meta-data
            android:name="aniyomix.contentWarning"
            android:value="${contentWarning}" />
        <meta-data
            android:name="aniyomix.extensionLib"
            android:value="${extensionLib}" />

        <!-- Legacy keys: still read by older hosts and by the repo indexer. -->
        <meta-data
            android:name="tachiyomi.animeextension.class"
            android:value="${extClass}" />
        <meta-data
            android:name="tachiyomi.animeextension.author"
            android:value="${author}" />
        <meta-data
            android:name="tachiyomi.animeextension.nsfw"
            android:value="${nsfw}" />
        <meta-data
            android:name="tachiyomi.animeextension.versionId"
            android:value="2" tools:replace="android:value" />
    </application>
</manifest>
