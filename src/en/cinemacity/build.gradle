ext {
    extName = 'CinemaCity'
    extClass = '.CinemaCity'
    extVersionCode = 2
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlist-utils'))
}
