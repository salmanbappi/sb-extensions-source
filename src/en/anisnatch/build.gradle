ext {
    extName = 'AniSnatch'
    extClass = '.AniSnatch'
    overrideVersionCode = 2
}

apply from: "$rootDir/common.gradle"

android {
    defaultConfig {
        manifestPlaceholders.appName = "AniSnatch"
    }
}

dependencies {
    implementation(project(":lib:streamwish-extractor"))
}
