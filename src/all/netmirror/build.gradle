ext {
    extName = 'NetMirror'
    extClass = '.NetMirror'
    author = '@salmanbappi'
    extVersionCode = 1000
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:cloudflare-interceptor"))
}
